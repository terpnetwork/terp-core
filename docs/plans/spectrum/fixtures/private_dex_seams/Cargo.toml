[package]
name = "private_dex_seams"
version = "0.1.0"
edition = "2021"
description = "Pure unit tests for private DEX seam math and oracle-bound rules (SPEC Domain D)"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
# std only — structural hashing for pool-nf / abstract leaf (no halo2)
sha2 = "0.10"
