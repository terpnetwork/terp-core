[package]
name = "compose_seams"
version = "0.1.0"
edition = "2021"
description = "Cross-crate pure L0 compose: authorize_bridge_mint → SeamNoteOutV0 (+ rcm) → optional SwapActionV0"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
sha2 = "0.10"
bridge_auth_seams = { path = "../bridge_auth_seams" }
seam_note_out = { path = "../seam_note_out" }
private_dex_seams = { path = "../private_dex_seams" }
